# git reset --soft is HEAD to INDEX ( commit is INDEX to HEAD )
```{bash}
$ git commit
$ git reset --soft HEAD^
$ #git reset --soft HEAD~2
$ git status
$ vi modify
$ git status
$ git add modify
$ git commit
```
